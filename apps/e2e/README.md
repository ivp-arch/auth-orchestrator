# @auth-orchestrator/e2e

End-to-end tests for Auth Orchestrator.

> 🚧 **Not yet implemented**. This package is a placeholder.
>
> Planned: Playwright tests against Keycloak (Docker), Auth0 (sandbox tenant),
> Okta (sandbox tenant) to validate full OAuth/OIDC flows.
> Timeline: Weeks 4-8 (alongside core development).

This package is `private: true` and will never be published to npm.
